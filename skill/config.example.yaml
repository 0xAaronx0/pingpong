# pingpong — Skill-Konfiguration. NUR NÖTIG, wenn du einen EIGENEN Broker
# betreibst — der öffentliche Broker (pingpong.kitescout.tech) ist im Skill
# voreingestellt. Override: diese Datei nach $PINGPONG_STATE_DIR/config.yaml
# (Default ~/.pingpong/config.yaml) kopieren, oder Umgebungsvariable
# PINGPONG_BROKER_URL setzen (env hat Vorrang).

broker_url: "https://pingpong.kitescout.tech"
